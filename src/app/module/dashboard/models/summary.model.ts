export interface ISummary {
  total_points?: string | number;
  total_income?: string | number;
  total_users?: number | number;
  total_available_income?: string | number;
  total_encash?: string | number;
}
